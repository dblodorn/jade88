import { 
  setResizeState,
  hasTouch,
  fontsLoaded,
  setInfoState,
  setCurrentPixel,
  setScrollDirection
} from './a_window_data'

import {
  updateQuantityInCart,
  removeLineItemInCart,
  handleCartClose,
  handleCartOpen,
  addVariantToCart
} from './a_cart'

import fetchApiData from './a_api-data'

export {
  setResizeState,
  hasTouch,
  fontsLoaded,
  setInfoState,
  updateQuantityInCart,
  removeLineItemInCart,
  handleCartClose,
  handleCartOpen,
  addVariantToCart,
  setCurrentPixel,
  setScrollDirection,
  fetchApiData
}
